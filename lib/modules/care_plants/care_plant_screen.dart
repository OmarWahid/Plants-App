import 'package:flutter/material.dart';

class CarePlantsScreen extends StatelessWidget {
  const CarePlantsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text('Care Plants Screen')),
      ],
    );
  }
}
